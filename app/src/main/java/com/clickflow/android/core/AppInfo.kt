package com.clickflow.android.core

/** Static app metadata. Kept in one place so screens stay in sync. */
object AppInfo {
const val APP_NAME = "ClickFlow Android"
const val VERSION_NAME = "0.1.0-prealpha"
const val STEP = "Step 68 — Template Manager: pure in-memory registry of capture templates (CaptureTemplate metadata + TemplateManager CRUD) with validation and JVM tests. Metadata/geometry only — no pixels, no disk, no capture/analysis. SafetyGate.canRunRealTap() still false"
const val SIMULATION_ONLY = true
}
