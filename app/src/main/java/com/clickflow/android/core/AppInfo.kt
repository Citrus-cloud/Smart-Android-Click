package com.clickflow.android.core

object AppInfo {
    const val VERSION_NAME = "0.1.0-prealpha"
    const val STEP = "Step 71 — On-device OCR stub (OcrProvider interface + StubOcrProvider + OcrController)"
}
