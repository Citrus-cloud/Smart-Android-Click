package com.clickflow.android.core

/** Static app metadata. Kept in one place so screens stay in sync. */
object AppInfo {
const val APP_NAME = "ClickFlow Android"
const val VERSION_NAME = "0.1.0-prealpha"
const val STEP = "Step 66 (Part 2) — Screen capture via MediaProjection: foreground ScreenCaptureService (single in-memory frame), consent Activity, capture UI. Capture only — no disk, no analysis. SafetyGate.canRunRealTap() still false"
const val SIMULATION_ONLY = true
}
