package com.clickflow.android.core

/** Static app metadata. Kept in one place so screens stay in sync. */
object AppInfo {
const val APP_NAME = "ClickFlow Android"
const val VERSION_NAME = "0.1.0-prealpha"
const val STEP = "Step 67 — Region Selector: normalized CaptureRegion geometry + framework-free RegionSelectorController (begin/update/commit/setRegion/clear) with JVM tests. Geometry only — no capture, no analysis. SafetyGate.canRunRealTap() still false"
const val SIMULATION_ONLY = true
}
