package com.clickflow.android.core

/** Static app metadata. Kept in one place so screens stay in sync. */
object AppInfo {
const val APP_NAME = "ClickFlow Android"
const val VERSION_NAME = "0.1.0-prealpha"
const val STEP = "Step 69 — Template matching engine: pure-Kotlin TemplateMatcher turning scored candidates into a confidence + highlight MatchResult (best-of selection, threshold decision). Decision/highlight only — no pixels, no tap. SafetyGate.canRunRealTap() still false"
const val SIMULATION_ONLY = true
}
