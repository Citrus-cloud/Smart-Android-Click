<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        ClickFlow Android — Step 52 foundation.
        SIMULATION-ONLY. No runtime permissions are requested at this step.
        No INTERNET, no Accessibility Service, no MediaProjection, no SYSTEM_ALERT_WINDOW.
        Future permissions are documented in docs/ANDROID_PERMISSIONS_PLAN.md but
        are intentionally NOT declared here yet.
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClickFlowAndroid">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ClickFlowAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
